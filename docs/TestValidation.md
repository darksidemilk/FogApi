# Testing Validation

A snapshot of the Pester test suite's most recent results and the parameter-set coverage report, generated by `Invoke-FogApiTests.ps1` and published here so it's readable without downloading CI artifacts. See [Contributing](Contributing.md) for how the suite itself works.

!!! note "Last generated 2026-07-19"
    Against `dev` after PR [#58](https://github.com/darksidemilk/FogApi/pull/58) merged. This is a point-in-time snapshot, not a live dashboard - it's refreshed manually alongside test-suite changes, not regenerated on every commit.

## Test results

**24 / 24** tests passed in 3.69s.

### `FogApi.Coverage.Tests.ps1` (1 tests)

- [x] `writes a coverage report to CoverageReportPath` (1.297s)

### `FogApi.Examples.Tests.ps1` (18 tests)

- [x] `Get-FogObject: Get-FogObject -type object -coreObject host` (0.188s)
- [x] `Get-FogHost: Get-FogHost -hostName MeowMachine` (0.039s)
- [x] `Get-FogHosts: Get-FogHosts` (0.023s)
- [x] `New-FogHost: New-FogHost -name "test-host" -macs "01:23:45:67:89:00"` (0.103s)
- [x] `Update-FogObject: $h = Get-FogHost -hostName MeowMachine; $h.ADUser = 'ldapBind'; Update-FogObject -type object -coreObject host -jsonData ($h | select-object aduser | ConvertTo-Json -Compress) -IDofObject $h.id -Verbose` (0.040s)
- [x] `New-FogObject: $hostID = 1234; $json = (@{"taskTypeID"='12';"deploySnapins"="-1";} | ConvertTo-Json); $result = New-FogObject -type objecttasktype -coreTaskObject host -jsonData $json -IDofObject "$hostID"; $result;` (0.024s)
- [x] `Send-FogWolTask: Send-FogWolTask -computername "MeowMachine"` (0.036s)
- [x] `Test-StringNotNullOrEmpty: Test-StringNotNullOrEmpty "An example"` (0.020s)
- [x] `Test-StringNotNullOrEmpty: $s = ""; Test-StringNotNullOrEmpty $s;` (0.015s)
- [x] `Test-StringNotNullOrEmpty: $str = " "; $str | Test-StringNotNullOrEmpty` (0.014s)
- [x] `Add-FogHostGroup: Add-FogHostGroup -hostID 42 -groupID 7` (0.184s)
- [x] `Add-FogHostGroup: Get-FogHost -hostName MeowMachine | Add-FogHostGroup -groupID 7` (0.064s)
- [x] `Remove-FogHostGroup: Remove-FogHostGroup -hostID 42 -groupID 3` (0.044s)
- [x] `Remove-FogHostGroup: Get-FogHost -hostName MeowMachine | Remove-FogHostGroup -groupID 5` (0.046s)
- [x] `Update-FogGroup: Update-FogGroup -groupID 7 -Description "Lab computers"` (0.029s)
- [x] `Update-FogGroup: $g = Get-FogGroupByName "TestGroup"; $g.description = "Updated via example"; $g | Update-FogGroup` (0.042s)
- [x] `Send-FogGroupTask: Send-FogGroupTask -groupID 7 -taskTypeID 14` (0.035s)
- [x] `Send-FogGroupTask: Get-FogGroupByName "TestGroup" | Send-FogGroupTask -taskTypeID 14 -StartAtTime ((Get-Date).AddHours(2))` (0.062s)

### `Invoke-FogApi.Tests.ps1` (5 tests)

- [x] `builds a normalized http:// uri for a bare server name` (0.198s)
- [x] `leaves an https:// server as-is` (0.035s)
- [x] `sends the fog-api-token and fog-user-token headers from settings` (0.028s)
- [x] `omits the body entirely when jsonData is empty` (0.035s)
- [x] `passes jsonData through as the body when supplied` (0.024s)

## Parameter-set coverage

**13 / 94** parameter sets across 65 functions have an annotated `Expected output:` example. This is informational only and never blocks a build - see [Contributing](Contributing.md#the-standing-goal-one-annotated-example-per-parameter-set) for the standing goal and the contributor policy around it.

### Add-FogHostGroup

- [x] `byHost`
- [x] `byId`

### Add-FogHostMac

- [ ] `byHost` - no example
- [ ] `byId` - example exists, not annotated with Expected output:

### Add-FogResultData

- [ ] `Default` - example exists, not annotated with Expected output:

### Approve-FogPendingMac

- [ ] `Default` - example exists, not annotated with Expected output:

### Deny-FogPendingMac

- [ ] `Default` - example exists, not annotated with Expected output:

### Disable-FogApiHTTPS

- [ ] `Default` - example exists, not annotated with Expected output:

### Dismount-WinEfi

- [ ] `Default` - example exists, not annotated with Expected output:

### Enable-FogApiHTTPS

- [ ] `Default` - example exists, not annotated with Expected output:

### Find-FogObject

- [ ] `Default` - no example

### Get-FogActiveTasks

- [ ] `Default` - example exists, not annotated with Expected output:

### Get-FogGroupAssociations

- [ ] `Default` - example exists, not annotated with Expected output:

### Get-FogGroupByName

- [ ] `Default` - example exists, not annotated with Expected output:

### Get-FogGroups

- [ ] `Default` - example exists, not annotated with Expected output:

### Get-FogHost

- [ ] `byID` - example exists, not annotated with Expected output:
- [x] `searchTerm`
- [ ] `serialNumber` - example exists, not annotated with Expected output:

### Get-FogHostAssociatedSnapins

- [ ] `byHost` - example exists, not annotated with Expected output:
- [ ] `byId` - example exists, not annotated with Expected output:

### Get-FogHostGroup

- [ ] `byHost` - no example
- [ ] `byId` - example exists, not annotated with Expected output:

### Get-FogHostMacs

- [ ] `byHostID` - example exists, not annotated with Expected output:
- [ ] `byHostObject` - example exists, not annotated with Expected output:

### Get-FogHostPendingMacs

- [ ] `byHost` - example exists, not annotated with Expected output:
- [ ] `byId` - example exists, not annotated with Expected output:

### Get-FogHosts

- [x] `Default`

### Get-FogImages

- [ ] `Default` - example exists, not annotated with Expected output:

### Get-FogInventory

- [ ] `Default` - example exists, not annotated with Expected output:

### Get-FogLog

- [ ] `Default` - example exists, not annotated with Expected output:

### Get-FogMacAddresses

- [ ] `Default` - example exists, not annotated with Expected output:

### Get-FogModules

- [ ] `Default` - example exists, not annotated with Expected output:

### Get-FogObject

- [ ] `Default` - no example

### Get-FogScheduledTasks

- [ ] `Default` - example exists, not annotated with Expected output:

### Get-FogSecsSinceEpoch

- [ ] `Default` - example exists, not annotated with Expected output:

### Get-FogServerSettings

- [ ] `Default` - example exists, not annotated with Expected output:

### Get-FogServerSettingsFile

- [ ] `Default` - example exists, not annotated with Expected output:

### Get-FogSetting

- [ ] `Default` - example exists, not annotated with Expected output:

### Get-FogSettings

- [ ] `Default` - example exists, not annotated with Expected output:

### Get-FogSnapinAssociations

- [ ] `Default` - example exists, not annotated with Expected output:

### Get-FogSnapins

- [ ] `Default` - example exists, not annotated with Expected output:

### Get-FogVersion

- [ ] `Default` - example exists, not annotated with Expected output:

### Get-LastImageTime

- [ ] `byHost` - example exists, not annotated with Expected output:
- [ ] `byHostId` - example exists, not annotated with Expected output:
- [ ] `bySN` - example exists, not annotated with Expected output:

### Get-WinBcdPxeID

- [ ] `Default` - example exists, not annotated with Expected output:

### Get-WinEfiMountLetter

- [ ] `Default` - example exists, not annotated with Expected output:

### Install-FogService

- [ ] `Default` - example exists, not annotated with Expected output:

### Invoke-FogApi

- [ ] `Default` - example exists, not annotated with Expected output:

### Mount-WinEfi

- [ ] `Default` - example exists, not annotated with Expected output:

### New-FogHost

- [ ] `custom` - no example
- [x] `default`

### New-FogObject

- [ ] `Default` - no example

### Receive-FogImage

- [ ] `now` - example exists, not annotated with Expected output:
- [ ] `now-byhost` - example exists, not annotated with Expected output:
- [ ] `schedule` - example exists, not annotated with Expected output:
- [ ] `schedule-byhost` - example exists, not annotated with Expected output:

### Remove-FogHostGroup

- [x] `byHost`
- [x] `byId`

### Remove-FogObject

- [ ] `Default` - no example

### Remove-UsbMac

- [ ] `byHost` - example exists, not annotated with Expected output:
- [ ] `byName` - example exists, not annotated with Expected output:

### Repair-FogSnapinAssociations

- [ ] `Default` - example exists, not annotated with Expected output:

### Reset-HostEncryption

- [ ] `HostID` - example exists, not annotated with Expected output:
- [ ] `HostObject` - example exists, not annotated with Expected output:

### Resolve-HostID

- [ ] `Default` - example exists, not annotated with Expected output:

### Send-FogGroupTask

- [x] `byId`
- [x] `byObj`

### Send-FogImage

- [ ] `byhost` - example exists, not annotated with Expected output:
- [ ] `byId` - example exists, not annotated with Expected output:

### Send-FogWolTask

- [ ] `byHostObject` - example exists, not annotated with Expected output:
- [x] `byname`

### Set-FogHostImage

- [ ] `Default` - example exists, not annotated with Expected output:

### Set-FogInventory

- [ ] `Default` - example exists, not annotated with Expected output:

### Set-FogServerSettings

- [ ] `default` - example exists, not annotated with Expected output:
- [ ] `prompt` - example exists, not annotated with Expected output:

### Set-FogServerSettingsFileSecurity

- [ ] `Default` - example exists, not annotated with Expected output:

### Set-FogSetting

- [ ] `byId` - example exists, not annotated with Expected output:
- [ ] `byname` - example exists, not annotated with Expected output:
- [ ] `byObj` - example exists, not annotated with Expected output:

### Set-FogSnapins

- [ ] `byId` - example exists, not annotated with Expected output:
- [ ] `byObject` - no example

### Set-WinToBootToPxe

- [ ] `Default` - example exists, not annotated with Expected output:

### Start-FogSnapin

- [ ] `byId` - example exists, not annotated with Expected output:
- [ ] `byId-byobj` - no example
- [ ] `byName` - example exists, not annotated with Expected output:
- [ ] `byName-byobj` - no example

### Start-FogSnapins

- [ ] `byid` - example exists, not annotated with Expected output:
- [ ] `byObj` - example exists, not annotated with Expected output:

### Test-FogVerAbove1dot6

- [ ] `Default` - example exists, not annotated with Expected output:

### Test-StringNotNullOrEmpty

- [x] `Default`

### Update-FogGroup

- [x] `byId`
- [x] `byObj`

### Update-FogObject

- [ ] `Default` - no example
