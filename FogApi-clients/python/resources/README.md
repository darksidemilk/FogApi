# resources/ — things shipped with the package

Empty until the Python client exists. Icons, licence, and anything else that
ships but is not generated. Kept outside `src/` so regeneration cannot touch
it.
