# src/ — the generator's scaffold

Empty until the Python client exists. When it does, this holds the generator's
own output and nothing else: it is regenerated wholesale, so anything
hand-written belongs a level up in `python/`, not here.

See `../README.md` and `spec/generators/README.md`.
