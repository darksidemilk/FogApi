# resources/ — things shipped with the module

Icons, licence, and anything else that ships but is not generated. Kept
outside `src/` so regenerating the scaffold cannot touch it.

The module's icons currently still live in `FogApi/icons/` alongside the
hand-written module; they move here when the helpers are ported to
`src/custom/`.
