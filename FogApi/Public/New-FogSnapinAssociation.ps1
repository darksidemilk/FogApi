function New-FogSnapinAssociation {
    <#
    .SYNOPSIS
    Creates a snapinassociation.

    .DESCRIPTION
    Creates a new snapinassociation object. Every writable field of the class is a named
    parameter, with the length and value constraints the server itself declares.
    Use -settings for a field a newer server has added that this module does not
    yet know about.

    .PARAMETER hostID
    Sets the hostID field (column saHostID).

    .PARAMETER snapinID
    Sets the snapinID field (column saSnapinID).

    .PARAMETER sequence
    Sets the sequence field (column saSequence).

    .PARAMETER settings
    A hashtable of raw field values, merged over the named parameters. An escape hatch for fields not yet modelled.

    .EXAMPLE
    New-FogSnapinAssociation -hostID 1 -snapinID 1

    Creates a snapinassociation and returns the created object.

    Expected output:
    { "id": 1, "hostID": 1, "snapinID": 1 }

    .NOTES
    Generated by spec/tools/New-FogApiFunctionFile.ps1 from operation 'createSnapinassociation'
    (POST /snapinassociation) in spec/fog-api-spec.json. Edit the spec, not this file.
    Requires the 'snapin.create' permission.
    #>

    [CmdletBinding()]
    param (
        [Parameter(Mandatory=$true)]
        [int]$hostID,
        [Parameter(Mandatory=$true)]
        [int]$snapinID,
        [Parameter()]
        [int]$sequence,
        [Parameter()]
        [hashtable]$settings
    )

    process {
        $payload = @{};
        if ($PSBoundParameters.ContainsKey('hostID')) { $payload.hostID = $hostID; }
        if ($PSBoundParameters.ContainsKey('snapinID')) { $payload.snapinID = $snapinID; }
        if ($PSBoundParameters.ContainsKey('sequence')) { $payload.sequence = $sequence; }
        # -settings wins, so a caller can reach a field this module does not
        # model yet without the named parameters fighting them for it.
        if ($PSBoundParameters.ContainsKey('settings')) {
            foreach ($key in $settings.Keys) { $payload[$key] = $settings[$key]; }
        }
        Write-Verbose "creating fog snapinassociation";
        return New-FogObject -type object -coreObject snapinassociation -jsonData ($payload | ConvertTo-Json -Compress);
    }

}
