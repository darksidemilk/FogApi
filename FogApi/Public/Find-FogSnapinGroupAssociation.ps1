function Find-FogSnapinGroupAssociation {
    <#
    .SYNOPSIS
    Finds snapingroupassociation objects matching a search term.

    .DESCRIPTION
    Matches the term across the snapingroupassociation class fields and returns the same envelope
    a list does.

    .PARAMETER stringToSearch
    Text to match across the class fields.

    .EXAMPLE
    Find-FogSnapinGroupAssociation -stringToSearch 'Example'

    Returns every snapingroupassociation matching "Example".

    Expected output:
    [ { "id": 1 } ]

    .NOTES
    Generated by spec/tools/New-FogApiFunctionFile.ps1 from operation 'searchSnapingroupassociation'
    (GET /snapingroupassociation/search/{item}) in spec/fog-api-spec.json. Edit the spec, not this file.
    Requires the 'snapin.view' permission.
    #>

    [CmdletBinding()]
    param (
        [Parameter(Mandatory=$true,Position=0)]
        [string]$stringToSearch
    )

    process {
        Write-Verbose "searching fog snapingroupassociation for $stringToSearch";
        return (Find-FogObject -coreObject snapingroupassociation -stringToSearch $stringToSearch).data;
    }

}
