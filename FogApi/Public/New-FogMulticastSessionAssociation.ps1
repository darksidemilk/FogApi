function New-FogMulticastSessionAssociation {
    <#
    .SYNOPSIS
    Creates a multicastsessionassociation.

    .DESCRIPTION
    Creates a new multicastsessionassociation object. Every writable field of the class is a named
    parameter, with the length and value constraints the server itself declares.
    Use -settings for a field a newer server has added that this module does not
    yet know about.

    .PARAMETER msID
    Sets the msID field (column msID).

    .PARAMETER taskID
    Sets the taskID field (column tID).

    .PARAMETER settings
    A hashtable of raw field values, merged over the named parameters. An escape hatch for fields not yet modelled.

    .EXAMPLE
    New-FogMulticastSessionAssociation -msID 1 -taskID 1

    Creates a multicastsessionassociation and returns the created object.

    Expected output:
    { "id": 1, "msID": 1, "taskID": 1 }

    .NOTES
    Generated by spec/tools/New-FogApiFunctionFile.ps1 from operation 'createMulticastsessionassociation'
    (POST /multicastsessionassociation) in spec/fog-api-spec.json. Edit the spec, not this file.
    Requires the 'task.create' permission.
    #>

    [CmdletBinding()]
    param (
        [Parameter(Mandatory=$true)]
        [int]$msID,
        [Parameter(Mandatory=$true)]
        [int]$taskID,
        [Parameter()]
        [hashtable]$settings
    )

    process {
        $payload = @{};
        if ($PSBoundParameters.ContainsKey('msID')) { $payload.msID = $msID; }
        if ($PSBoundParameters.ContainsKey('taskID')) { $payload.taskID = $taskID; }
        # -settings wins, so a caller can reach a field this module does not
        # model yet without the named parameters fighting them for it.
        if ($PSBoundParameters.ContainsKey('settings')) {
            foreach ($key in $settings.Keys) { $payload[$key] = $settings[$key]; }
        }
        Write-Verbose "creating fog multicastsessionassociation";
        return New-FogObject -type object -coreObject multicastsessionassociation -jsonData ($payload | ConvertTo-Json -Compress);
    }

}
