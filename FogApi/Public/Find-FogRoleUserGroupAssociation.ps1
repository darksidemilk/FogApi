function Find-FogRoleUserGroupAssociation {
    <#
    .SYNOPSIS
    Finds roleusergroupassociation objects matching a search term.

    .DESCRIPTION
    Matches the term across the roleusergroupassociation class fields and returns the same envelope
    a list does.

    .PARAMETER stringToSearch
    Text to match across the class fields.

    .EXAMPLE
    Find-FogRoleUserGroupAssociation -stringToSearch 'Example'

    Returns every roleusergroupassociation matching "Example".

    Expected output:
    [ { "id": 1, "name": "ExampleRoleusergroupassociation" } ]

    .NOTES
    Generated by spec/tools/New-FogApiFunctionFile.ps1 from operation 'searchRoleusergroupassociation'
    (GET /roleusergroupassociation/search/{item}) in spec/fog-api-spec.json. Edit the spec, not this file.
    Requires the 'usergroup.view' permission.
    FOG 1.6 only; FOG 1.5 does not serve this operation.
    #>

    [CmdletBinding()]
    param (
        [Parameter(Mandatory=$true,Position=0)]
        [string]$stringToSearch
    )

    process {
        Write-Verbose "searching fog roleusergroupassociation for $stringToSearch";
        return (Find-FogObject -coreObject roleusergroupassociation -stringToSearch $stringToSearch).data;
    }

}
