function Find-FogMulticastSessionAssociation {
    <#
    .SYNOPSIS
    Finds multicastsessionassociation objects matching a search term.

    .DESCRIPTION
    Matches the term across the multicastsessionassociation class fields and returns the same envelope
    a list does.

    .PARAMETER stringToSearch
    Text to match across the class fields.

    .EXAMPLE
    Find-FogMulticastSessionAssociation -stringToSearch 'Example'

    Returns every multicastsessionassociation matching "Example".

    Expected output:
    [ { "id": 1 } ]

    .NOTES
    Generated by spec/tools/New-FogApiFunctionFile.ps1 from operation 'searchMulticastsessionassociation'
    (GET /multicastsessionassociation/search/{item}) in spec/fog-api-spec.json. Edit the spec, not this file.
    Requires the 'task.view' permission.
    #>

    [CmdletBinding()]
    param (
        [Parameter(Mandatory=$true,Position=0)]
        [string]$stringToSearch
    )

    process {
        Write-Verbose "searching fog multicastsessionassociation for $stringToSearch";
        return (Add-FogTypeName -InputObject (Find-FogObject -coreObject multicastsessionassociation -stringToSearch $stringToSearch).data -TypeName 'FogApi.Multicastsessionassociation');
    }

}
