function New-FogUserGroupMember {
    <#
    .SYNOPSIS
    Creates a usergroupmember.

    .DESCRIPTION
    Creates a new usergroupmember object. Every writable field of the class is a named
    parameter, with the length and value constraints the server itself declares.
    Use -settings for a field a newer server has added that this module does not
    yet know about.

    .PARAMETER name
    Sets the name field (column ugmName).

    .PARAMETER usergroupID
    Sets the usergroupID field (column ugmGroupID).

    .PARAMETER userID
    Sets the userID field (column ugmUserID).

    .PARAMETER settings
    A hashtable of raw field values, merged over the named parameters. An escape hatch for fields not yet modelled.

    .EXAMPLE
    New-FogUserGroupMember -usergroupID 1 -userID 1

    Creates a usergroupmember and returns the created object.

    Expected output:
    { "id": 1, "usergroupID": 1, "userID": 1 }

    .NOTES
    Generated by spec/tools/New-FogApiFunctionFile.ps1 from operation 'createUsergroupmember'
    (POST /usergroupmember) in spec/fog-api-spec.json. Edit the spec, not this file.
    Requires the 'usergroup.create' permission.
    FOG 1.6 only; FOG 1.5 does not serve this operation.
    #>

    [CmdletBinding()]
    param (
        [Parameter()]
        [ValidateLength(0,60)]
        [string]$name,
        [Parameter(Mandatory=$true)]
        [int]$usergroupID,
        [Parameter(Mandatory=$true)]
        [int]$userID,
        [Parameter()]
        [hashtable]$settings
    )

    process {
        $payload = @{};
        if ($PSBoundParameters.ContainsKey('name')) { $payload.name = $name; }
        if ($PSBoundParameters.ContainsKey('usergroupID')) { $payload.usergroupID = $usergroupID; }
        if ($PSBoundParameters.ContainsKey('userID')) { $payload.userID = $userID; }
        # -settings wins, so a caller can reach a field this module does not
        # model yet without the named parameters fighting them for it.
        if ($PSBoundParameters.ContainsKey('settings')) {
            foreach ($key in $settings.Keys) { $payload[$key] = $settings[$key]; }
        }
        Write-Verbose "creating fog usergroupmember";
        return New-FogObject -type object -coreObject usergroupmember -jsonData ($payload | ConvertTo-Json -Compress);
    }

}
