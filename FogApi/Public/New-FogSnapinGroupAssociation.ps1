function New-FogSnapinGroupAssociation {
    <#
    .SYNOPSIS
    Creates a snapingroupassociation.

    .DESCRIPTION
    Creates a new snapingroupassociation object. Every writable field of the class is a named
    parameter, with the length and value constraints the server itself declares.
    Use -settings for a field a newer server has added that this module does not
    yet know about.

    .PARAMETER snapinID
    Sets the snapinID field (column sgaSnapinID).

    .PARAMETER storagegroupID
    Sets the storagegroupID field (column sgaStorageGroupID).

    .PARAMETER primary
    Sets the primary field (column sgaPrimary).

    .PARAMETER settings
    A hashtable of raw field values, merged over the named parameters. An escape hatch for fields not yet modelled.

    .EXAMPLE
    New-FogSnapinGroupAssociation -snapinID 1 -storagegroupID 1

    Creates a snapingroupassociation and returns the created object.

    Expected output:
    { "id": 1, "snapinID": 1, "storagegroupID": 1 }

    .NOTES
    Generated by spec/tools/New-FogApiFunctionFile.ps1 from operation 'createSnapingroupassociation'
    (POST /snapingroupassociation) in spec/fog-api-spec.json. Edit the spec, not this file.
    Requires the 'snapin.create' permission.
    #>

    [CmdletBinding()]
    param (
        [Parameter(Mandatory=$true)]
        [int]$snapinID,
        [Parameter(Mandatory=$true)]
        [int]$storagegroupID,
        [Parameter()]
        [ValidateSet('0','1')]
        [string]$primary,
        [Parameter()]
        [hashtable]$settings
    )

    process {
        $payload = @{};
        if ($PSBoundParameters.ContainsKey('snapinID')) { $payload.snapinID = $snapinID; }
        if ($PSBoundParameters.ContainsKey('storagegroupID')) { $payload.storagegroupID = $storagegroupID; }
        if ($PSBoundParameters.ContainsKey('primary')) { $payload.primary = $primary; }
        # -settings wins, so a caller can reach a field this module does not
        # model yet without the named parameters fighting them for it.
        if ($PSBoundParameters.ContainsKey('settings')) {
            foreach ($key in $settings.Keys) { $payload[$key] = $settings[$key]; }
        }
        Write-Verbose "creating fog snapingroupassociation";
        return New-FogObject -type object -coreObject snapingroupassociation -jsonData ($payload | ConvertTo-Json -Compress);
    }

}
