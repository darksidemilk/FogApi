function New-FogMacAddressAssociation {
    <#
    .SYNOPSIS
    Creates a macaddressassociation.

    .DESCRIPTION
    Creates a new macaddressassociation object. Every writable field of the class is a named
    parameter, with the length and value constraints the server itself declares.
    Use -settings for a field a newer server has added that this module does not
    yet know about.

    .PARAMETER hostID
    Sets the hostID field (column hmHostID).

    .PARAMETER mac
    Sets the mac field (column hmMAC).

    .PARAMETER description
    Sets the description field (column hmDesc).

    .PARAMETER pending
    Sets the pending field (column hmPending).

    .PARAMETER primary
    Sets the primary field (column hmPrimary).

    .PARAMETER clientIgnore
    Sets the clientIgnore field (column hmIgnoreClient).

    .PARAMETER imageIgnore
    Sets the imageIgnore field (column hmIgnoreImaging).

    .PARAMETER settings
    A hashtable of raw field values, merged over the named parameters. An escape hatch for fields not yet modelled.

    .EXAMPLE
    New-FogMacAddressAssociation -hostID 1 -mac 'example'

    Creates a macaddressassociation and returns the created object.

    Expected output:
    { "id": 1, "hostID": 1, "mac": "example" }

    .NOTES
    Generated by spec/tools/New-FogApiFunctionFile.ps1 from operation 'createMacaddressassociation'
    (POST /macaddressassociation) in spec/fog-api-spec.json. Edit the spec, not this file.
    Requires the 'host.create' permission.
    #>

    [CmdletBinding()]
    param (
        [Parameter(Mandatory=$true)]
        [int]$hostID,
        [Parameter(Mandatory=$true)]
        [ValidateLength(0,59)]
        [string]$mac,
        [Parameter()]
        [string]$description,
        [Parameter()]
        [ValidateSet('0','1')]
        [string]$pending,
        [Parameter()]
        [ValidateSet('0','1')]
        [string]$primary,
        [Parameter()]
        [ValidateSet('0','1')]
        [string]$clientIgnore,
        [Parameter()]
        [ValidateSet('0','1')]
        [string]$imageIgnore,
        [Parameter()]
        [hashtable]$settings
    )

    process {
        $payload = @{};
        if ($PSBoundParameters.ContainsKey('hostID')) { $payload.hostID = $hostID; }
        if ($PSBoundParameters.ContainsKey('mac')) { $payload.mac = $mac; }
        if ($PSBoundParameters.ContainsKey('description')) { $payload.description = $description; }
        if ($PSBoundParameters.ContainsKey('pending')) { $payload.pending = $pending; }
        if ($PSBoundParameters.ContainsKey('primary')) { $payload.primary = $primary; }
        if ($PSBoundParameters.ContainsKey('clientIgnore')) { $payload.clientIgnore = $clientIgnore; }
        if ($PSBoundParameters.ContainsKey('imageIgnore')) { $payload.imageIgnore = $imageIgnore; }
        # -settings wins, so a caller can reach a field this module does not
        # model yet without the named parameters fighting them for it.
        if ($PSBoundParameters.ContainsKey('settings')) {
            foreach ($key in $settings.Keys) { $payload[$key] = $settings[$key]; }
        }
        Write-Verbose "creating fog macaddressassociation";
        return (Add-FogTypeName -InputObject (New-FogObject -type object -coreObject macaddressassociation -jsonData ($payload | ConvertTo-Json -Compress)) -TypeName 'FogApi.Macaddressassociation');
    }

}
