function New-FogGroupAssociation {
    <#
    .SYNOPSIS
    Creates a groupassociation.

    .DESCRIPTION
    Creates a new groupassociation object. Every writable field of the class is a named
    parameter, with the length and value constraints the server itself declares.
    Use -settings for a field a newer server has added that this module does not
    yet know about.

    .PARAMETER hostID
    Sets the hostID field (column gmHostID).

    .PARAMETER groupID
    Sets the groupID field (column gmGroupID).

    .PARAMETER settings
    A hashtable of raw field values, merged over the named parameters. An escape hatch for fields not yet modelled.

    .EXAMPLE
    New-FogGroupAssociation -hostID 1 -groupID 1

    Creates a groupassociation and returns the created object.

    Expected output:
    { "id": 1, "hostID": 1, "groupID": 1 }

    .NOTES
    Generated by spec/tools/New-FogApiFunctionFile.ps1 from operation 'createGroupassociation'
    (POST /groupassociation) in spec/fog-api-spec.json. Edit the spec, not this file.
    Requires the 'group.create' permission.
    #>

    [CmdletBinding()]
    param (
        [Parameter(Mandatory=$true)]
        [int]$hostID,
        [Parameter(Mandatory=$true)]
        [int]$groupID,
        [Parameter()]
        [hashtable]$settings
    )

    process {
        $payload = @{};
        if ($PSBoundParameters.ContainsKey('hostID')) { $payload.hostID = $hostID; }
        if ($PSBoundParameters.ContainsKey('groupID')) { $payload.groupID = $groupID; }
        # -settings wins, so a caller can reach a field this module does not
        # model yet without the named parameters fighting them for it.
        if ($PSBoundParameters.ContainsKey('settings')) {
            foreach ($key in $settings.Keys) { $payload[$key] = $settings[$key]; }
        }
        Write-Verbose "creating fog groupassociation";
        return (Add-FogTypeName -InputObject (New-FogObject -type object -coreObject groupassociation -jsonData ($payload | ConvertTo-Json -Compress)) -TypeName 'FogApi.Groupassociation');
    }

}
